machine:
  python:
    version: 3.4.3

dependencies:
  post:
    - pip install -e .

test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/py.test
    - py.test tests --junitxml=$CIRCLE_TEST_REPORTS/py.test/moola.xml
